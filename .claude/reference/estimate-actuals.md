# Estimate Actuals — Guess vs. Reality

> Auto-generated by `estimate-log.sh --rollup` on 2026-09-07T17:36:31Z.
> Source: `~/.claude/estimate-log.jsonl` (164 rows for `auerbachb/claude-code-config`).
> Regenerate: `estimate-log.sh --rollup --repo auerbachb/claude-code-config`

This table is the measured counterpart to the tier table in
[`time-estimates.md`](time-estimates.md), whose values come from
`est = coding + rounds × 30`. Re-tune a tier's **round count** from the
rounds column below rather than by hand. Do **not** derive the 30-min
per-round unit as minutes ÷ rounds off this table: the minutes span
claim → merge (`pr_created` fallback), so they are the right comparison
for the planning bound but include the coding term the per-round unit
excludes. That unit is re-derived only from an open → merge measurement.
The published tier table stays authoritative for tiers with fewer than ~5 rows.

## Recalibrated Tier → Time Table

| Tier | Measured Median | ~P90 | Measured Rounds (median) | Rounds ~P90 | Rows | Rows w/ rounds | Outliers |
|------|----------------|------|--------------------------|-------------|------|----------------|----------|
| Unknown | 75.25 min | 1014.09 min | 2 | 7 | 163 | 163 | 0 |
| Standard | 9.68 min | 9.68 min | 1 | 1 | 1 | 1 | 0 |

> **Rounds** = the PR total commit count, the proxy for the review-and-CI
> rounds `time-estimates.md` prices at 30 min each. It counts the pre-open
> implementation commit too, so it runs about one high — the same way the
> tier table was calibrated, so the two stay comparable. Same rows,
> same outlier rule as the minutes columns: outliers are included in the
> quantiles and flagged rather than dropped. **Rows w/ rounds** is the
> subset carrying a measured round count — rows written before the field
> existed show `—` here and still count in the minutes columns. Run
> `estimate-log.sh --backfill` to patch them.
>
> **Outliers**: actual duration exceeded planning bound × 3 (likely
> unattended/overnight). Included in quantiles; flagged for visibility.

## Recent Guesses vs. Actuals

| PR | Issue | Tier | Estimate | Actual | Rounds | Δ | Source |
|----|-------|------|----------|--------|--------|---|--------|
| #1382 | #1380 | Standard | 45–90 min | 9.68 min | 1 | -80.3 min | pr_created |

> **Δ** = actual − planning bound. Negative = completed under budget.
>
> **Source**: `claim_comment` = accurate start (issue claim marker);
> `pr_created` = approximate fallback (PR creation time).
>
> _Generated by `estimate-log.sh --rollup`. Do not edit manually._
